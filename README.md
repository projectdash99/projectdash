# ProjectDash

A Multi-Model AI Chat Platform.

## Setup

1. Run `npm install` to install dependencies.
2. Add your Gemini API key to `.env`: `GEMINI_API_KEY="your-key-here"`.
3. Run `npm run dev` to start the development server.
